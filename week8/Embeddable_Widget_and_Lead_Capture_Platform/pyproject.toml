[project]
name = "flyrank-capstone-widget-platform"
version = "0.2.0"
description = "FlyRank backend capstone: hardened embeddable widget submission platform"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
