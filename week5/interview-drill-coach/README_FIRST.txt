FL-07 INTERVIEW DRILL COACH — NEXT STEPS

1. Open the project in VS Code or a terminal.
2. Create and activate a Python 3.11+ virtual environment.
3. Run: pip install -r requirements.txt
4. Copy .env.example to .env and insert your OpenAI API key.
5. Run: pytest
6. Test privately:
   python -m app.main --provider openai --topic "backend projects" --difficulty medium --questions 2
7. Record the same command in one raw, unedited screen capture.
8. Follow recording_guide.md and submission_checklist.md.

Do not upload .env or expose the API key in the recording.
